import { Card, CardTitle } from "@/components/ui/card";

export default function newQuestions() {
    return (
        <Card>
            <CardTitle>Questions Pending Approval</CardTitle>
        </Card>
    )
}